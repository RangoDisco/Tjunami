export class Fact {
    public title: string
    public date: string
    public desc: string
    constructor(title: string, date: string, desc: string) {
        this.title = title
        this.date = date
        this.desc = desc
    }
}
